import { CarouselItemNamePipe } from './carousel-item-name.pipe';

describe('CarouselItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CarouselItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
