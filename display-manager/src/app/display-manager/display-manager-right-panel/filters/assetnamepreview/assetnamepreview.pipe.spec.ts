import { AssetnamePreviewPipe } from './assetnamepreview.pipe';

describe('AssetnamePreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetnamePreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
