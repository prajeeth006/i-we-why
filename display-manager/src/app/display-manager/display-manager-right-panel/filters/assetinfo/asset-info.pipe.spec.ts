import { AssetInfoPipe } from './asset-info.pipe';

describe('AssetInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
