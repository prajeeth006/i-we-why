export enum ResultCode {
    Win = 'WIN',
    Place = 'PLACE',
    Lose = 'LOSE'
}

export enum Category {
    categoryName = 'TRAP',
    categoryKey1 = 19,
    categoryKey2 = 21,
}

export enum ResultCount {
    latestThree = 4,
    latestSix = 7,
}

export enum EachWay {
   winOnly ="WIN ONLY"
}

