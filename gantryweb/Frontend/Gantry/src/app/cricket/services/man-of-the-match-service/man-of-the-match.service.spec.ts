// import { ManOfTheMatchService } from './man-of-the-match.service';
// import { MockManOfTheMatchData } from '../../mocks/mock-cricket-data';

describe('ManOfTheMatchService', () => {
    // let service: ManOfTheMatchService;
    // let mockCommonUtilityService;
    // let mockdata: MockManOfTheMatchData;

    // beforeEach(() => {
    //     mockCommonUtilityService = jasmine.createSpyObj(['setOddsFormat', 'setHomeDrawOrAwayDetails', 'setCricketPageTitle']);
    //     service = new ManOfTheMatchService(mockCommonUtilityService);
    //     mockdata = new MockManOfTheMatchData();
    // });

    // it('get price from odds when no odds are passed', () => {
    //     expect(ManOfTheMatchService.getPriceFromOdds("")).toBe(0);
    // });

    // it('get price from odds', () => {
    //     expect(ManOfTheMatchService.getPriceFromOdds("4/10")).toBe(0.4);
    // });

    // it('set man of the match player list', () => {
    //     expect(service.setManOfTheMatchPlayers(mockdata.playeroneDetail, mockdata.manOfTheMatchPlayers)).toBe(mockdata.manOfTheMatchPlayers);
    // });

    // it('check man of the match list is sorted or not', () => {
    //     expect(service.setManOfTheMatchPlayers(mockdata.playeroneDetail, mockdata.manOfTheMatchPlayers).manOfTheMatchPlayerList[0]).toBe(mockdata.playeroneDetail);
    // });

    // it('check length of man of the match list  when first player is passed', () => {
    //     expect(service.setManOfTheMatchPlayers(mockdata.playeroneDetail, mockdata.noManOfTheMatch).manOfTheMatchPlayerList.length).toBeLessThanOrEqual(1);
    // });

    // it('check length of man of the match list when more than 4 players are set', () => {
    //     expect(service.setManOfTheMatchPlayers(mockdata.playeroneDetail, mockdata.fourManOfTheMatchPlayers).manOfTheMatchPlayerList.length).toBeLessThanOrEqual(4);
    // });

    // it('set man of the match players in panel', () => {
    //     expect(service.setManOfTheMatchDetails("MAN OF THE MATCH", mockdata.cricketTemplateResult, mockdata.selections)).toBe(mockdata.cricketTemplateResult);
    // });

    // it('check length of the man of the match panel', () => {
    //     expect(service.setManOfTheMatchDetails("MAN OF THE MATCH", mockdata.cricketTemplateResult, mockdata.selections).manOfTheMatchPlayerPanel.manOfTheMatchPlayerList.length).toBeGreaterThanOrEqual(1);
    // });
});