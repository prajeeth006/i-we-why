// import { InningsLeadService } from './first-innings-lead.service';
// import { MockInningsLeadData } from '../../mocks/mock-cricket-data';

describe('InningsLeadService', () => {
    // let service: InningsLeadService;
    // let mockCommonUtilityService;
    // let mockdata: MockInningsLeadData;

    beforeEach(() => {
        // mockCommonUtilityService = jasmine.createSpyObj(['setOddsFormat', 'setHomeDrawOrAwayDetails', 'setCricketPageTitle']);
        // service = new InningsLeadService(mockCommonUtilityService);
        // mockdata = new MockInningsLeadData();
    });

    // it('setting first innings lead', () =>  {
    //     expect(service.setFirstInningsLeadPanel(mockdata.teamDetail, mockdata.teams, mockdata.cricketTemplateResult)).toBe(mockdata.teams);
    // });

    // it('check home team details after setting first innings lead', () =>  {
    //     expect(service.setFirstInningsLeadPanel(mockdata.homeTeamDetail, mockdata.teams, mockdata.cricketTemplateResult).homeTeamDetails).toBe(mockdata.homeTeamDetail);
    // });

    // it('set first innings lead with no event passed', () =>  {
    //     expect(service.setFirstInningsLeadPanel(mockdata.teamDetail, mockdata.teams, mockdata.noEventCricketTemplateResult).homeTeamDetails.betName).toBe("HomeTeam");
    // });

    // it('set innings lead panel', () => {
    //     expect(service.setFirstInningsLeadDetails("1ST INNINGS LEAD", mockdata.cricketTemplateResult, mockdata.selections)).toBe(mockdata.cricketTemplateResult);
    // });
});