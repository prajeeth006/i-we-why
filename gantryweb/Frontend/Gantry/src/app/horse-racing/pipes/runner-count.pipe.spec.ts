import { RunnerCountPipe } from './runner-count.pipe';

describe('RunnerCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RunnerCountPipe();
    expect(pipe).toBeTruthy();
  });
});
