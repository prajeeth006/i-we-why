import { MarketPriceTransformPipe } from './market-price-transform.pipe';

describe('MarketPriceTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketPriceTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
