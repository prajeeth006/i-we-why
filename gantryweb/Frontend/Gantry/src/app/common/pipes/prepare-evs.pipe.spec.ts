import { PrepareEvsPipe } from './prepare-evs.pipe';

describe('PrepareEvsPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareEvsPipe();
    expect(pipe).toBeTruthy();
  });
});
