import { RunnerPositionSuffixPipe } from './runner-position-suffix.pipe';

describe('RunnerPositionSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new RunnerPositionSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
