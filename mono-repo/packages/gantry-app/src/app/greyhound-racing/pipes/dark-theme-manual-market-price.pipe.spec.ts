import { DarkThemeManualMarketPricePipe } from './dark-theme-manual-market-price.pipe';

describe('DarkThemeManualMarketPricePipe', () => {
    it('create an instance', () => {
        const pipe = new DarkThemeManualMarketPricePipe();
        expect(pipe).toBeTruthy();
    });
});
