import { NcastTypePipe } from './ncast-type.pipe';

describe('NcastTypePipe', () => {
    it('create an instance', () => {
        const pipe = new NcastTypePipe();
        expect(pipe).toBeTruthy();
    });
});
