import { TruncateAndFormatPipe } from './truncate-and-format.pipe';

describe('TruncateAndFormatPipe', () => {
    it('create an instance', () => {
        const pipe = new TruncateAndFormatPipe();
        expect(pipe).toBeTruthy();
    });
});
