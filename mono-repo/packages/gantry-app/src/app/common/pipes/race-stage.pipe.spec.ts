import { RaceStagePipe } from './race-stage.pipe';

describe('RaceStagePipe', () => {
    it('create an instance', () => {
        const pipe = new RaceStagePipe();
        expect(pipe).toBeTruthy();
    });
});
