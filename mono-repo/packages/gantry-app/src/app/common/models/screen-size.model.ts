export enum ScreenWidth {
    large = 1920,
    multidesktop = 960,
    small = 760,
}
export enum ScreenHeight {
    large = 1080,
    multidesktop = 540,
}

export enum ScreenType {
    half = 'half',
    quad = 'quad',
    full = 'full',
}
