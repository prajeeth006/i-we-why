export interface ActiveWagerDetails {
    productId: string;
    lastWageredTimeStamp: string;
    blockWager: boolean;
    blockWagerForSeconds: string;
    code: number;
    codeDescription: string;
    firstActivatedTime: string;
}
