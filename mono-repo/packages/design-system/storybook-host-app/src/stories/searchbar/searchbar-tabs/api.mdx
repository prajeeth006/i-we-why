import { ArgTypes, Canvas, Controls, Meta, Story } from '@storybook/blocks';
import { Source } from '@storybook/blocks';

import * as DsSearchBarStories from '../searchbar.component.stories';

## Features

-   Customizable Search Icon: You can project a custom search icon using an ng-template `#searchIcon`, or use the default one provided.
-   Clear Button: A clear button is supported and can be projected using the ng-template `#closeIcon`, or use the default one provided.
