import { Canvas, Controls, Meta, Story } from '@storybook/blocks';

import * as DsAccordionStories from '../accordion.component.stories';

### Default

<Canvas of={DsAccordionStories.Default}></Canvas>

### Custom Toggle

An accordion with a custom toggle using `*dsAccordionTrigger="let isOpen"`.

<Canvas of={DsAccordionStories.WithCustomToggle}></Canvas>
